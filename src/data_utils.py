import pandas as pd
from pathlib import Path


def load_historical_prices(csv_path: Path) -> pd.DataFrame:
    # TODO: Update this project to estimate parameters of Spot Prices with actual UK Power system prices!
    pass